# inktalk 0.4.2 产品发布说明

inktalk 0.4.2 改进每日记录中的热词选择与排序。

## 改进

- 热词选择支持按住滑动，可正向或反向连续选择。
- 从已选字符开始拖动时，连续取消对应范围。
- 空格继续作为热词分段边界，不会被误加入热词。
- 最新确认的热词默认放在热词编辑列表最上方。
- 最新确认的热词同步置于 ASR 热词请求优先队列最前面。
- 批量加入时保留用户选择顺序，并按大小写不敏感规则去重。

## 验证边界

单元测试、Lint 和构建不能代替滑动方向、跨行选择、外层页面滚动与 TalkBack 的真机触控验收。
