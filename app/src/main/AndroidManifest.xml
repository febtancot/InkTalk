<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:supportsRtl="true">

        <service
            android:name=".InkTalkIME"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <activity
            android:name=".history.InputHistoryActivity"
            android:exported="true"
            android:label="@string/history_title"
            android:theme="@style/Theme.InkTalk">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".settings.SettingsActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.InkTalk" />

        <activity
            android:name=".settings.HotwordSettingsActivity"
            android:exported="false"
            android:label="@string/hotwords_title"
            android:theme="@style/Theme.InkTalk" />

        <activity
            android:name=".settings.BackupSettingsActivity"
            android:exported="false"
            android:label="@string/backup_title"
            android:theme="@style/Theme.InkTalk" />
    </application>
</manifest>
